main: pro.dracarys.LocketteX.LocketteX
name: LocketteX
authors: [Valentina_pro]
prefix: "${project.name}"
version: "${project.version}"
description: "${project.description}"
api-version: '1.13'
softdepend: [Vault,Factions,FactionsX,Lands,MassiveCore,Towny,HolographicDisplays]
commands:
  lockettex:
    description: Main Command for LocketteX
    usage: "Usage: /<command>"
    aliases: [protect]