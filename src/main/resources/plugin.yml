name: LocketteX
author: Dracarys
prefix: "${project.name}"
description: "${project.description}"
version: "${project.version}"
main: pro.dracarys.LocketteX.LocketteX
softdepend: [Vault,Factions,Towny,HolographicDisplays]
commands:
  lockettex:
    description: Main Command for LocketteX
    usage: "Usage: /<command>"
    aliases: [protect]